/**
 * Labelled dropdown.
 *
 * Thin alias over the shared form library primitive — see `ui/form/SelectInput`.
 */
export {
  SelectInput as Select,
  type SelectInputProps as SelectProps,
  type SelectOption,
} from './form/SelectInput';
