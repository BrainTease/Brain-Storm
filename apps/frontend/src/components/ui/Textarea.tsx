/**
 * Labelled multi-line text input.
 *
 * Thin alias over the shared form library primitive — see `ui/form/TextArea`.
 */
export { TextArea as Textarea, type TextAreaProps as TextareaProps } from './form/TextArea';
