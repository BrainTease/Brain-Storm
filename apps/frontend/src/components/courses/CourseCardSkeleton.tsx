/**
 * CourseCardSkeleton — course grid loading placeholder.
 *
 * The full implementation now lives in `@/components/ui/Skeleton` as the
 * canonical `CourseCardSkeleton` export (issue #972).  This file re-exports
 * it so existing imports under `components/courses/` continue to resolve.
 *
 * Do not add new skeleton logic here — extend `ui/Skeleton.tsx` instead.
 */
export { CourseCardSkeleton } from '@/components/ui/Skeleton';
